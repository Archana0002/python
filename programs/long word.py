a = ['hiba','rebeka','kavya','suparna']
'''long = ""
max_l=0
for word in a:
    if len(word) > max_l:
        max_l = len(word)
        long = word

print(long)'''

l=""
max=0
for word in a:
    if(len(word)>max):
        max = len(word)
        long = word
